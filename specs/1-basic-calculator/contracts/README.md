# API Contracts for Basic Calculator

This feature implements a command-line interface (CLI) application. As such, it does not expose any external APIs or require formal API contracts. The interaction is directly through the command line.
